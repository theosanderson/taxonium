apiVersion: v1
kind: Service
metadata:
  name: {{ include "viral-usher-web.fullname" . }}
  labels:
    {{- include "viral-usher-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "viral-usher-web.selectorLabels" . | nindent 4 }}
