[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "setuptools_scm>=6.2"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "taxoniumtools/src/taxoniumtools/_version.py"
root = "../"
#relative_to = "__file__"
